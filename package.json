{
  "name": "xynera-ia-planner",
  "version": "1.0.0",
  "scripts": {
    "start:backend": "cd backend && npm install && npm run start",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "start": "npx concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
